spring:
  application:
    name: storage-demo
management:
  security:
    enabled: false
google:
  credentials:
    bucket-name: ${vcap.services.storage-service.credentials.bucket_name}
    private-key: ${vcap.services.storage-service.credentials.PrivateKeyData}